% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_image_features.R
\name{download_imageTCGA_data}
\alias{download_imageTCGA_data}
\title{Download data from U24 Cancer Genomics (HoverNet or ProvGigaPath)}
\usage{
download_imageTCGA_data(
  dataset = c("hovernet", "provgigapath"),
  level = c("slide_level", "tile_level"),
  tumor_types = NULL,
  subfolder = NULL,
  pattern = NULL,
  dest_dir = "downloads",
  dry_run = FALSE
)
}
\arguments{
\item{dataset}{One of "hovernet" or "provgigapath".}

\item{level}{For ProvGigaPath, one of "slide_level" or "tile_level". Ignored for HoverNet.}

\item{tumor_types}{Character vector of TCGA cancer types (e.g., c("TCGA_OV", "TCGA_BRCA")).
If NULL, all available tumor types are included.}

\item{subfolder}{For HoverNet, one of "h5ad", "json", or "thumb".
If NULL, downloads all subfolders.}

\item{pattern}{Optional regular expression to match filenames (e.g. "geojson", "tsv", "h5ad").}

\item{dest_dir}{Local directory for downloads (default = "downloads").}

\item{dry_run}{Logical; if TRUE, only lists files without downloading.}
}
\value{
Character vector of downloaded (or listed) files.
}
\description{
This function navigates the data repository structure for HoverNet and
ProvGigaPath outputs, allowing selective download by tumor type and data level.
}
\examples{
\dontrun{
# List HoverNet JSON files for OV
download_imageTCGA_data("hovernet", tumor_types = "TCGA_OV",
                        subfolder = "json", pattern = "geojson", dry_run = TRUE)

# Download slide-level ProvGigaPath features for OV and BRCA
download_imageTCGA_data("provgigapath", level = "slide_level",
                        tumor_types = c("TCGA_OV", "TCGA_BRCA"))
}
}
